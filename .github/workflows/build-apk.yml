name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:          # lets you trigger a build manually from the Actions tab

# Opt into Node.js 24 now to prevent the workflow from breaking on June 16th, 2026
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Android build tools (Gradle) require Java 17
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Node 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli

      # Create the config file directly to bypass ALL interactive CLI prompts.
      - name: Configure Bubblewrap JDK and SDK paths
        run: |
          mkdir -p ~/.bubblewrap
          cat > ~/.bubblewrap/config.json << EOF
          {
            "jdkPath": "$JAVA_HOME",
            "androidSdkPath": "$ANDROID_SDK_ROOT"
          }
          EOF

      # The Ubuntu 24.04 runner places sdkmanager in cmdline-tools/latest/bin, 
      # but Bubblewrap expects it in tools/bin or bin. 
      # We create a symlink so Bubblewrap can find it and pass validation.
      - name: Fix Bubblewrap SDK Path
        run: |
          if [ ! -e "$ANDROID_SDK_ROOT/bin" ]; then
            ln -s "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" "$ANDROID_SDK_ROOT/bin"
          fi

      # Recreate the keystore from the base64 secret
      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > decibel.keystore

      # Write twa-manifest.json directly — this REPLACES "bubblewrap init"
      - name: Write twa-manifest.json
        run: |
          cat > twa-manifest.json << 'EOF'
          {
            "packageId": "com.mayenm.decibel",
            "host": "mayenm.github.io",
            "name": "Decibel",
            "launcherName": "Decibel",
            "display": "standalone",
            "orientation": "default",
            "themeColor": "#000000",
            "navigationColor": "#000000",
            "backgroundColor": "#000000",
            "startUrl": "/Decibel/",
            "iconUrl": "https://mayenm.github.io/Decibel/icon-512.png",
            "splashScreenFadeOutDuration": 300,
            "shortcuts": [],
            "generatorApp": "bubblewrap-cli",
            "webManifestUrl": "https://mayenm.github.io/Decibel/manifest.json",
            "fallbackType": "customtabs",
            "features": {},
            "alphaDependencies": { "enabled": false },
            "enableNotifications": false,
            "appVersionCode": 1,
            "appVersion": "1.0.0",
            "signingKey": {
              "path": "./decibel.keystore",
              "alias": "${{ secrets.KEY_ALIAS }}"
            },
            "minSdkVersion": 21,
            "rotationEnabled": true,
            "fullScopeUrl": "https://mayenm.github.io/Decibel/",
            "locationDelegation": { "enabled": false }
          }
          EOF

      # Update the TWA project to generate Android files (gradlew, etc.) and checksum.txt.
      # The --skipVersionUpgrade flag ensures it runs 100% non-interactively in CI.
      - name: Update TWA Project
        run: bubblewrap update --skipVersionUpgrade

      # Use Bubblewrap's native CI environment variables to securely pass the passwords.
      # No interactive prompts will trigger here because checksum.txt now exists.
      - name: Build APK
        env:
          BUBBLEWRAP_KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          BUBBLEWRAP_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: bubblewrap build

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: "*.apk"
